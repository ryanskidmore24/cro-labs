diff a/src/app/tests/[id]/page.tsx b/src/app/tests/[id]/page.tsx	(rejected hunks)
@@ -1,5 +1,6 @@
-import { prisma } from "@/lib/db";
-import { notFound } from "next/navigation";
+import { prisma } from "@/lib/prisma";
+import { getSession } from "@/lib/auth/session";
+import { notFound, redirect } from "next/navigation";
 import TestDetailClient from "./TestDetailClient";
 
 interface Props {
@@ -7,17 +8,20 @@ interface Props {
 }
 
 export default async function TestDetailPage({ params }: Props) {
+  const session = await getSession();
+  if (!session) redirect("/login");
+
   const { id } = await params;
 
   const test = await prisma.test.findUnique({
-    where: { id },
+    where: { id, organizationId: session.orgId },
     include: {
       variants: true,
       results: {
         include: { variant: { select: { name: true, isControl: true } } },
         orderBy: { computedAt: "desc" },
       },
-      guardrails: true,
+      guardrailMetrics: true,
       owner: { select: { name: true, email: true } },
       hypotheses: { orderBy: { createdAt: "desc" } },
     },
